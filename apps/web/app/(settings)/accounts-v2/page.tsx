"use client";

// Temporary stub to isolate build error. Original page backed up during investigation.
export default function AccountsSettingsPage() {
  return (
    <div className="p-6 text-sm text-gray-600">
      (settings)/accounts-v2/page.tsx temporarily disabled for build isolation.
    </div>
  );
}
// (original component temporarily removed)
